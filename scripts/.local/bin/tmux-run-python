#!/usr/bin/env zsh
current_window=$(tmux display-message -p '#W')

if [[ "$current_window" == "run" ]]; then
  tmux last-window
  exit 0
fi

if ! tmux select-window -Tt run 2>/dev/null; then
  tmux new-window -n run
fi

tmux send-keys -t run "run_cli" C-m

